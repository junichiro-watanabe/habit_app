<% if object.errors.any? %>
  <div class="alert alert-danger">
    <ul>
      <% if yield(:title) == "Sign up" %>
        <% user_error_messages(object.errors.full_messages).each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      <% end %>
    </ul>
  </div>
<% end %>
