<% provide(:title, @title) %>
<% provide(:page, "group") %>

<div class="index">
    <h2><%= @heading %></h2>
    <%= render 'shared/group_index' %>
    <%= eval("will_paginate @groups, params: {controller: #{@controller}, action: #{@action}}") %>
</div>
