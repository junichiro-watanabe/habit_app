<% if current_user?(@user) %>
  <% provide(:page, "user_show") %>
<% else %>
  <% provide(:page, "group") %>
<% end %>


<h2><%= "#{@user.name} さんの いいねした投稿" %></h2>
<%= render 'shared/feed_items' %>
<%= will_paginate @feed_items %>
